[project]
name = "kin"
version = "0.1.0"
description = "Offline multilingual family reunification agent"
requires-python = ">=3.11"
dependencies = [
    "ollama",
    "requests",
    "python-dotenv",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
